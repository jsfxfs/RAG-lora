# 金融年报语料 RAG + LoRA 项目 — 状态交接文档

- **文档日期**：2026-07-10
- **最近更新**：2026-07-12 — ① 删除 `generate_qa.py` 中遗留的未调用函数（`extract_text`/`chunk_text`/`_split_sections` 及 `PdfReader`/`SECTION_PAT`/`_sliding`），仅保留仍被主流程使用的 `digit_ratio`；切块逻辑全部由 `clean_report` 提供。② 给 API 调用加 **2 并发**：原逐块串行（`time.sleep(0.3)`）改为 `ThreadPoolExecutor(max_workers=2)`，`--workers N` 可调（默认 2，山大共享代理省着用）；结果按块顺序组装，纯数字密集块仍跳过 QA。③ 新增**显式子集控制**与**断点续存**：`--limit N` 只跑前 N 份；每跑完一份即落盘，重跑时按 `source` 跳过已完成的 PDF（`--fresh` 可强制从头）。④ **LoRA 数据清洗**：`clean_qa.py` 用全局正则去除 `qa_alpaca/qa_sharegpt` 回答中"根据(报告原文/财报片段/年报片段/片段原文/…)，"类检索语境残留（句首句中都清，保留真实答案），1351 条→23 条（残留 23 条为无"根据"前缀的裸"片段原文/报表原文"自然回指，合法不处理）；原始文件备份为 `qa_alpaca.raw.json`/`qa_sharegpt.raw.json`。详见 §4 `generate_qa.py` 说明与 `clean_qa.py`。
- **用途**：给另一个对话框 / 另一个会话接手本项目用。本文档力求**如实**，区分「已完成」「说了要做但没做」「代码写了但未生效」。
- **项目根目录**：`E:\Work\self-learning\LLM\bagu-pape-backup\finance_reports\`

---

## 0. 一句话目标

为「应用开发岗面试差异化项目」准备一份**金融年报垂域语料**：同一批 50 份上市公司年报 PDF，既做 **RAG 检索索引**，又造 **LoRA 微调问答数据**（即学习计划里「方案 C：RAG + LoRA 组合拳」）。核心要求是**自己造垂域数据**，不用 alpaca/gsm8k 这类 demo 数据集。

---

## 1. 整体架构（4 阶段流水线）

```
① 下载        ② 抽取+清洗+结构化      ③ LoRA 微调数据        ④ RAG 检索数据
巨潮API ──▶ PDF ──▶ 纯文本+表格(清洗) ──▶ qa_alpaca/sharegpt ──▶ rag_chunks
                  │                          (喂 LLaMA-Factory)     (建向量索引)
                  └─ 切块(按章节)
```

对应目录（已建立）：

| 阶段 | 文件夹 | 内容 |
|---|---|---|
| ① | `01_年报PDF/` | 50 份中文年报 PDF（原始素材，已下好） |
| ② | `02_文本抽取/` | 每份报告的清洗文本 `_clean.txt` + 结构化表格 `_tables.json` + 切块 `_chunks.json`（**目前仅有 pilot 旧产物，未全量生成**） |
| ③ | `03_LoRA数据/` | `qa_alpaca.json` + `qa_sharegpt.json`（喂 LoRA） |
| ④ | `04_RAG数据/` | `rag_chunks.json`（进 RAG 索引） |

---

## 2. 已完成（已验证可跑）

| # | 事项 | 证据 / 状态 |
|---|---|---|
| 1 | **巨潮批量下载脚本** `download_cninfo_reports.py` | 已跑通，50 份 PDF 落在 `01_年报PDF/`（约 133 MB） |
| 2 | 下载过程踩坑已解决 | ① 接口单页最多 30 条→翻页累计；② 时间戳是毫秒→`ts_to_date()` 转日期；③ PDF 真地址要拼 `static.cninfo.com.cn/`；④ 按 `secCode` 去重（同公司只留 1 条）；⑤ 过滤「摘要/英文」，只留中文年报全文 |
| 3 | **文档→QA 生成器** `generate_qa.py` | 已测通：**已接入清洗+表格结构化**，pilot 跑满第 1 份（中简科技）→ 产出 **626 条 QA**（alpaca/sharegpt 各 626）+ **223 个 RAG chunk**（含 37 个结构化财报表格块），0 调用失败、0 误跳，质量良好（详见 `pilot_clean.log`）。注：接入清洗前旧 pilot 曾产出 677 QA / 255 chunk（不清洗），见 §4。 |

> **备注（脚本 docstring 已与代码不符，已修）**：`generate_qa.py` 顶部用法注释原写「`--pilot` 限量 8 块」，实际 pilot 分支 `MAX_CHUNKS=None`（跑满第 1 份）；本次已把 docstring 改成「跑满第 1 份」。以代码为准。
| 4 | **模型切换为 DeepSeek-V4-Flash** | `.env` 已锁定 `SDU-AI/DeepSeek-V4-Flash`（输入 1 元/M tokens、输出 2 元/M tokens、上下文 64K） |
| 5 | **禁用 Kimi** | 目录下无任何 Kimi 残留引用（grep 确认）；`.env` 只有 DeepSeek 一个模型名 |
| 6 | **切块逻辑：按章节切**（非滑动窗口） | 由 `clean_report.py` 的 `chunk_text` / `_split_sections` 实现「按年报章节标题切 + 长章节内滑动窗口兜底」（`generate_qa.py` 早年自带的不清洗版 `extract_text`/`chunk_text`/`_split_sections` 已于 2026-07-12 删除，主流程只用 `clean_report`）。pilot 验证每块是语义完整的章节片段 |
| 7 | **修复「数字过滤把财务报表全丢」的 bug** | 原 `digit_ratio>=0.35` 整段丢弃会把利润表/资产负债表干掉，已在切块层移除该过滤，财务数据保留进 RAG |
| 8 | **清洗+表格结构化 已接入并验证（P0 完成）** | `generate_qa.py` 现调用 `clean_report`：`extract_pages→clean_pages→extract_tables→build_chunks`；产出 `_clean.txt`/`_tables.json`/`_chunks.json`；表格块也生成 QA。pilot 实测：626 QA / 223 chunk（37 表格块）/ 0 失败。详见 `pilot_clean.log` |

---

## 3. 说了要做、但「没做 / 没做完 / 没生效」（诚实清单 — 重点）

> 这节是交接的核心。下面的每一项都是**之前对话里承诺过、但实际没落地**的，接手会话要优先补。

### 3.1 「按章节切分」—— 有过「说一套写一套」的历史
- **最初口头承诺**：「按章节切分（识别管理层讨论与分析等标题切块）」。
- **实际第一版代码**：是固定 1000 字滑动窗口，**没有按章节**。被用户发现后才改成真·按章节切。
- **现状**：✅ 现已在 `generate_qa.py` 落地并 pilot 验证。但这段历史要记下来，免得误以为「一开始就是对的」。

### 3.2 清洗（cleaning）—— ✅ **已于 2026-07-10 补齐并验证（原 ⚠️ 最关键项）**
- **历史说法**：「清洗很轻，主要靠 prompt 防幻觉兜底」→ 实际几乎没有清洗，用户要求加 6 步真清洗。
- **当初没做的**：① `generate_qa.py` 未集成 `clean_report`（集成编辑曾失败）；② `clean_report.py` 从未本地验证；③ 跑主流程仍是不清洗旧逻辑。
- **现状（2026-07-10 已解决）**：`generate_qa.py` 已改为调用 `clean_report` 全流程；`clean_report.py` 验证时发现并修复了一个崩溃 bug（`chunk_text` 不能处理 `_split_sections` 返回的元组），另加了表格过滤（见 3.3）。pilot 实测清洗生效、表格数字正确、0 失败。详见 §2 #8 与 `pilot_clean.log`。

### 3.3 表格结构化（pdfplumber）—— ✅ **已接入并验证（2026-07-10）**
- `clean_report.py` 的 `extract_tables()` 用 pdfplumber 把线框表转 Markdown，打「资产负债表/利润表/现金流量表/主要会计数据」等标签。
- **验证发现 pdfplumber 会把栏式正文误判为表**：单份年报原始抽到 ~260 张表（86% 是未命中的噪声"表格"）。已加过滤：只保留标题命中 STMT_KW 的**真实财报表格**（中简科技实测 263→37 张，全是三大表/主要会计数据类）。表格块现已进 RAG 并生成 QA（pilot 37 个表格块全部参与）。

### 3.4 章节重排（解决「财务报告窜前」）—— ✅ **已接入（2026-07-10）**
- `clean_report.py` 的 `reorder_sections()`（按「第 X 节」序号重排）已在 `build_chunks` 内被调用，随清洗一并接入主流程。当前切块顺序按章节号排，不再窜前。

### 3.5 全量跑 50 份 —— 从未执行
- 只 pilot 过 1 份（中简科技）。全量 50 份 **一直没跑**，原因是 API 端点是**山东大学共享代理**，要省着用、需用户确认额度。

### 3.6 `03_LoRA数据/` 和 `04_RAG数据/` 下的 json —— 已重生成为**清洗后 pilot 产物**（仍仅 1 份）
- 当前 `qa_alpaca.json` / `qa_sharegpt.json` / `rag_chunks.json` 来自 **1 份报告（中简科技）、且已清洗+含表格**（2026-07-10 pilot 重生成）。
- 全量 50 份跑完后，这些文件会被**整体覆盖**为全量版本。

---

## 4. 当前每个脚本的真实状态

### `download_cninfo_reports.py` ✅ 完成可用
- 功能：巨潮批量下最近 50 份中文年报全文。
- 状态：已跑，50/50 落盘。一般不再需要动。

### `generate_qa.py` ✅ 已接入清洗（2026-07-10）
- **当前行为**：`clean_report.extract_pages → clean_pages → extract_tables → build_chunks`（清洗+表格结构化+按章节重排切块）→ 调 DeepSeek 生成 QA；每份写 `_clean.txt` / `_tables.json` / `_chunks.json`。
- 表格块（`【…第N页】` 前缀，`is_table=True`）**也生成 QA**；仅"非表格的纯数字密集块"仍跳过 QA（防幻觉，但进 RAG）。pilot 实测 0 个误跳。
- 注意：原 `extract_text` / `chunk_text` / `_split_sections`（generate_qa 自带、不清洗版本）现已被 `clean_report` 取代，**已于 2026-07-12 从 `generate_qa.py` 删除**（连同已无用的 `PdfReader` 导入与 `SECTION_PAT`/`_sliding` 辅助函数）。`generate_qa.py` 现仅保留仍被主流程调用的 `digit_ratio`，其余切块逻辑全部来自 `clean_report`。
- **API 调用已支持并发（2026-07-12）**：由原来的逐块串行（`time.sleep(0.3)`）改为 `ThreadPoolExecutor(max_workers=WORKERS)`，`WORKERS` 默认 2、`--workers N` 可调；同一份报告内的块并发生成 QA，结果按块顺序组装（保持 `alpaca`/`sharegpt` 顺序稳定），纯数字密集块仍跳过。全量跑前如需提速可酌情调大 `--workers`，但端点为山大共享代理、务必先确认额度。
- **显式子集 + 断点续存（2026-07-12）**：`--limit N` 只跑前 N 份（`--pilot` 等同只跑第 1 份）；每跑完一份即把 `qa_alpaca/qa_sharegpt/rag_chunks` 落盘，重跑时按 `source` 跳过已完成的 PDF，中途崩溃不丢已完成产出。`--fresh` 可忽略已有产出、在目标子集内从头重跑。⚠️ 注意：若换了切块参数（如 `--max-chunks`）想重切某份，需 `--fresh` 或删掉对应产出，否则会被当作"已完成"跳过。
- **LoRA 数据清洗（2026-07-12，`clean_qa.py`）**：`qa_alpaca.json`/`qa_sharegpt.json` 的回答约 19% 带"根据片段原文/根据财报片段/根据年报片段/根据原文/根据上述"等检索语境残留（生成时注入），散落句首与句中，SFT 后模型会"凭空引用原文"。`clean_qa.py` 用**全局正则**删除"根据(报告原文/财报片段/年报片段/片段原文/财报原文/年报原文/报告/财报/年报/片段/原文/上述)，"短语（句首句中都清），保留后续真实答案；约 1351 条 → 23 条。**残留 23 条均为无"根据"前缀的裸"片段原文/报表原文"自然回指**（如"该报表缺少片段原文"），属合法表述，不处理。仅删"根据+文档类词"，保留"根据企业会计准则/公司章程/《公司法》"等合法用法。原始文件备份为 `qa_alpaca.raw.json`/`qa_sharegpt.raw.json`，可直接对比。`--dry` 只预览不写盘。RAG 的 `rag_chunks.json` 无需清洗（检索块本就是原文）。

### `clean_report.py` ✅ 已验证并接入（2026-07-10）
- 全部函数已验证可用：
  - `extract_pages(path)` — 按页抽原始文本（保留页边界，供跨页检测页眉页脚）
  - `clean_pages(pages)` — **步骤 1-4**：NFKC 归一 + 删页码行；跨页检测并删页眉页脚；行断裂修复；目录/套话清理
  - `extract_tables(path)` — **步骤 5**：pdfplumber 抽线框表 → Markdown；**已加过滤只留真实财报表格**（标题命中 STMT_KW），丢弃栏式正文误判的噪声表
  - `reorder_sections(text)` — **步骤 6 前半**：按「第 X 节」序号重排（解决窜前）
  - `dedup(chunks)` — **步骤 6 后半**：按规整后文本去重
  - `chunk_text` / `build_chunks(text, tables)` — 重排后章节切块 + 表格块插入 + 去重
- 验证中修了一个崩溃 bug：`chunk_text` 原不能处理 `_split_sections` 返回的 `(header, body)` 元组（已改为合并标题+正文再切）。

### `.env` ✅
```
ANTHROPIC_BASE_URL=https://xplt.sdu.edu.cn:4000
ANTHROPIC_AUTH_TOKEN=sk-BwTS1KiAIYVHaHKfpsvoiQ
ANTHROPIC_DEFAULT_HAIKU_MODEL=SDU-AI/DeepSeek-V4-Flash
```
- ⚠️ 含 token，**别提交、别外传**。
- ⚠️ 端点是**山大共享代理**，跑全量前需确认额度。

---

## 5. 后续 TODO（按优先级）

| 优先级 | 任务 | 说明 |
|---|---|---|
| P0 ✅ | **验证 `clean_report.py`** | **已完成（2026-07-10）**：修崩溃 bug + 加表格过滤；对 2 份 PDF 验证，页眉页脚清掉、表格数字正确（见 `_test_*` 产物与 §2 #8） |
| P0 ✅ | **把 `clean_report` 接入 `generate_qa.py`** | **已完成（2026-07-10）**：用 `build_chunks(clean, tables)` 替换原逻辑；写 `_clean.txt`/`_tables.json`；表格块生成 QA（非表格纯数字块仍跳过）。pilot 跑通：626 QA / 223 chunk / 0 失败 |
| P1 | **决定是否重排章节** | 接入 `reorder_sections`（已在 clean_report 内，build_chunks 已调用） |
| P1 | **全量跑 50 份** | 先确认山大代理额度；建议分批（每次 10 份 × 5 轮）。约 1.4 万次 API 调用，成本极低（< 2 元）但次数多 |
| P2 | **数据质控** | 去重 + 格式校验 + 人工抽检 50-100 条（面试能讲「我做了一致性抽检」） |
| P2 | **覆盖旧 pilot 产物** | 全量跑完后，`03_LoRA数据/` 和 `04_RAG数据/` 的 json 会被重新生成 |

---

## 6. 关键约束 / 坑（给接手会话的提醒）

1. **Python 解释器**：必须用受管 venv 的 python，否则缺 `pypdf` / `pdfplumber`。
   - 解释器：`C:\Users\21031\.workbuddy\binaries\python\envs\default\Scripts\python.exe`
   - 库装在 `C:\Users\21031\.workbuddy\binaries\python\envs\default\Lib\site-packages\`（已装 `pypdf`、`pdfplumber`）
   - 项目数据（PDF/脚本/产出）全在 **E 盘**，库在 **C 盘用户目录**，互不污染。
2. **API 端点**：`xplt.sdu.edu.cn:4000`（山大共享代理，Anthropic 兼容 `/v1/messages` 格式）。**别无脑全量跑**，先确认额度。
3. **模型**：只用 `SDU-AI/DeepSeek-V4-Flash`。**禁用 Kimi**（用户明确要求）。
4. **上下文 64K 足够**：管线每次只发 1 个 ~1000 字块，单 call 约 600 tokens，远在限额内。
5. **年报是文本型 PDF，无 OCR 步骤**：pypdf / pdfplumber 直接抽字，不需要 OCR。若以后换扫描件语料才需接 OCR。
6. **金融数据别让模型死背会过期的数值**：QA 偏定性/业务/定义类；具体数字来自原文，且表格结构化后数字才准。
7. **ST / *ST 公司**：50 份里含多家 ST 公司（亚振、正平、步森等），文本丰富，对语料多样性是好事，但别声称能做投资建议。

---

## 7. 给接手会话的快速上手命令

```bash
# 进项目目录
cd E:/Work/self-learning/LLM/bagu-pape-backup/finance_reports

# 用受管 venv 的 python
PY="C:/Users/21031/.workbuddy/binaries/python/envs/default/Scripts/python.exe"

# ① 先本地验证清洗(不耗 API) —— 看 clean_report 是否靠谱
$PY -c "
import clean_report as cr, os
pdf='01_年报PDF'
fn=sorted(f for f in os.listdir(pdf) if f.endswith('.pdf'))[0]
pages=cr.extract_pages(os.path.join(pdf,fn))
clean=cr.clean_pages(pages)
tables=cr.extract_tables(os.path.join(pdf,fn))
print('清洗文本字数:', len(clean))
print('抽到表格张数:', len(tables))
print('首张表标题:', tables[0]['title'] if tables else None)
open('02_文本抽取/_test_clean.txt','w',encoding='utf-8').write(clean)
import json; json.dump(tables, open('02_文本抽取/_test_tables.json','w',encoding='utf-8'), ensure_ascii=False, indent=2)
print('已写出 _test_clean.txt / _test_tables.json 供人工检查')
"

# ② 验证 OK 后, 把 clean_report 接入 generate_qa.py(见第 5 节 P0)

# ③ 先 pilot 1 份确认接入后质量
$PY generate_qa.py --pilot

# ④ 确认无误 + 山大代理额度允许后, 全量
$PY generate_qa.py
```

---

## 8. 一句话给接手者

> 下载（✅）、QA 生成骨架（✅）、**清洗+表格结构化（✅ 已于 2026-07-10 验证并接入，pilot 626 QA / 223 chunk / 37 表格块 / 0 失败）** 都通了。**下一步最大动作是「全量跑 50 份」**——先确认山大代理额度，再分批跑，这个语料项目就基本成型了。
