# 实验数据分析汇报：本地模型 vs API 做 RAG 生成

> 日期：2026-07-27
> 结果文件：`eval_retrieval_results.json`（API 后端）、`eval_local/results_base.json`、`eval_local/results_rank16.json`（本地后端）
> 作废数据：`eval_local/*.bak_oom`（首轮 OOM 污染，仅留档）

---

## 1. 实验目的

回答两个问题：

1. **换生成模型，检索消融的结论还成立吗？** —— 验证 50→67→77→85 的消融曲线反映的是检索质量，而非 DeepSeek 这一个生成器的偶然偏好。
2. **LoRA 微调（sft_rank16）在 RAG 场景下值不值？** —— LoRA 是按闭卷问答训的，放进 RAG（开卷）里对比纯 base，看有没有增益。

## 2. 实验设置

| 项 | 配置 |
|---|---|
| 考卷 | 新版评测 QA（3108 条带 source/company），seed=42 抽 **100 题**，三组实验同一份考卷 |
| 检索配置 | hr（hybrid + bge-reranker-large 精排，20 候选取 6）、hr_route（+公司路由）；API 后端另有 vec / hybrid 两档 |
| 生成后端 A | DeepSeek-V4-Flash（API） |
| 生成后端 B | Qwen3-4B 纯 base（本地，卡 1，bf16，贪心解码，关思考模式） |
| 生成后端 C | Qwen3-4B + LoRA sft_rank16（本地，卡 3，其余同上） |
| 裁判 | 统一 DeepSeek-V4-Flash 二值判分（LLM-as-Judge），三组可直接同表对比 |
| evidence 指标 | 参考答案中 ≥4 位数字是否出现在检索上下文（不依赖裁判，纯检索指标） |
| 硬件 | 2 张 16G 卡；bge/reranker/生成模型全 GPU（`--retrieval-device cuda`） |

## 3. 实验结果

### 3.1 API 后端：4 配置检索消融（背景基线）

| 配置 | acc | evidence | errors | 增量归因 |
|---|---|---|---|---|
| vec | 50.0% | 84.9% | 0 | 纯向量基线 |
| hybrid | 67.0% | 84.9% | 0 | +BM25/RRF，**+17pp** |
| hr | 77.0% | 92.5% | 0 | +reranker 精排，**+10pp** |
| hr_route | **85.0%** | **96.2%** | 0 | +公司路由，**+8pp**（修 15 题退 7 题） |

### 3.2 本地 vs API：三方对比（核心结果）

**全量 100 题**（OOM 计 0，压低本地绝对分）：

| 配置 | base | rank16 | API |
|---|---|---|---|
| hr | 66.0% | 63.0% | 77.0% |
| hr_route | 75.0% | 76.0% | 85.0% |

**净 acc**（排除 OOM 题后 ~90 题，"API 同子集"= DeepSeek 在完全相同题目上的成绩，公平对比）：

| 配置 | base | rank16 | API 同子集 |
|---|---|---|---|
| hr | 72.5% | 70.8% | 79.1% |
| hr_route | **83.3%** | **82.6%** | **86.7%** |

**逐题胜负**（双方都作答的题）：

- hr：共同 88 题，base 独对 8 / rank16 独对 7 → 打平
- hr_route：共同 89 题，base 独对 4 / rank16 独对 4 → 完全打平

**三方交叉**（hr_route 共同 89 题）：三方全对 68，三方全错 5，仅 API 对 6。

**答案风格**（hr_route，字符中位长度）：base 206 / rank16 **73** / API 160 —— rank16 学到了训练数据"直接给短答案"的风格。

### 3.3 OOM 题说明

- base/rank16 各有 9~11 题 CUDA OOM（差 700~800M 显存），全部是**最长上下文题**（如“白银有色按单项计提坏账准备的应收账款明细”这类长表格题）；
- 两张卡 OOM 的是**同一批题**（同一考卷同一检索），因此 base vs rank16 的对比不受影响；
- 这批题连 API 也只答对 5~7/10，属于考卷里的困难题。

### 3.4 检索耗时实测（单题平均，`bench_retrieval_time.py`，20 题预热后计时）

| 配置 | CPU（API 评测环境） | GPU（本地评测环境） |
|---|---|---|
| vec | 0.09s | 0.02s |
| hybrid | 0.11s | 0.04s |
| hr | 4.45s | 0.62s |
| hr_route | 4.18s | 0.62s |

- 耗时大头全在 reranker 精排（20 候选打分，占 ~97%），召回本身 <0.1s；GPU 上精排约 7 倍加速；
- **公司路由零延迟代价**（路由后候选池变小，hr_route 反而略快）：+8pp 准确率免费拿；
- 端到端单题 ≈ 检索 0.6~4.4s + 生成（API 3~10s / 本地 GPU 5~15s；此为经验估算非埋点实测，生成延迟与卡算力/代理服务负载强相关，不作硬指标），延迟瓶颈在生成侧。

### 3.5 recall@k 直算（2026-07-28 补，`eval_validity/01_recall/eval_recall.py`，纯本地零 API）

同一批 100 题（seed=42，与 §3.1 同子集），两级命中：**doc 级** = top-k 内出现 gold source PDF 的块（分母 100）；**chunk 级** = 命中参考答案引用的【块N】对应块（仅 21 题答案带块号标注，分母 21，n 小区间宽±~20pp，看趋势不看绝对值）：

| 配置 | doc@6 | chunk@6 | doc@20 | chunk@20 | acc（§3.1） |
|---|---|---|---|---|---|
| vec | 93.0% | 42.9% | 100% | 47.6% | 50.0% |
| hybrid | 94.0% | 52.4% | 100% | 66.7% | 67.0% |
| hr | 92.0% | 66.7% | 100% | 66.7% | 77.0% |
| hr_route | **100%** | **81.0%** | 100% | **85.7%** | **85.0%** |

四个要点：

1. **chunk@6 与 acc 几乎同步**（42.9→52.4→66.7→81.0 vs 50→67→77→85）——消融曲线的 acc 增益确实来自检索命中率提升，结论 1 的检索侧直接证据；
2. **找对公司≠找对块**：vec 的 doc@6 已 93% 但 chunk@6 仅 42.9%——15 家共库定位公司不难（doc@20 全档 100%，早饱和），难的是把正确的那一块排进前 6；
3. **rerank 与路由的分工被 recall 切开了**：hybrid→hr 的 chunk@20 不变（66.7%=66.7%，同一候选集）而 chunk@6 +14.3pp——精排只负责把 gold 块从 20 候选拉进前 6；hr→hr_route 的 chunk@20 +19pp（66.7→85.7）——路由改善的是候选集合本身；
4. **reranker 会跨公司串扰**：hr 的 doc@6（92%）反而低于 hybrid（94%），精排偶尔把别家公司的相似块排进前 6，公司路由恰好治这个（hr_route doc@6=100%）。

### 3.6 拒答/幻觉评测（2026-07-28 补，`eval_validity/02_refusal/`，hr_route + API 生成）

30 题库内无答案的问题（自造考卷 `questions.json`）：15 题**库外公司**（茅台/宁德等知名 A 股，不在 B 侧 15 家也不在 50 家 PDF 池）+ 15 题**编造事项**（B 侧 15 家每家 1 题，专挑“存在必披露”的大事；含 3 题硬负例——语料里有相关关键词但事件为假，如华盛锂电释义表真有“Enchem 指 韩国亿恩科”）。裁判三分类 R 拒答/H 幻觉/V 模糊，**30 题全量人工复核**：

| 范围 | n | 拒答 R | 幻觉 H | 模糊 V | 拒答率 | 幻觉率 |
|---|---|---|---|---|---|---|
| 全部 | 30 | 30 | 0 | 0 | **100%** | **0%** |
| 库外公司 | 15 | 15 | 0 | 0 | 100% | 0% |
| 编造事项 | 15 | 15 | 0 | 0 | 100% | 0% |

要点：

- **人工复核与裁判 30/30 一致**，数字不依赖裁判可信度；且未加任何防幻觉提示词（现有生成 prompt 的“严格基于片段”已够用，预留的 --guard 对照组无需再跑）；
- **硬负例全过**：#28 未顺嘴答 Enchem；#23 白银有色检索召回了真实的“巴西铜金矿 4.3 亿美元收购”，模型准确区分“非智利锂矿”未张冠李戴；#21 山西焦煤还引原文反驳（“锚定煤炭主业”与“关闭全部煤矿”矛盾）；#5 片段里有未标公司名的净利润数字，模型拒绝归属；
- **路由行为正确**：库外公司 15 题全部 routed=None（不误触发），编造事项 15 题全部正确路由；
- **口径提醒**：本评测生成器为 API（DeepSeek-V4-Flash)；本地 Qwen3-4B 的拒答能力未测（当前无卡跑本地 LLM，后续可选补）。

### 3.7 口语化改写测试（2026-07-28 补，`eval_validity/03_colloquial/`，hr_route + API 生成）

针对"考卷近亲繁殖"质疑（QA 出题/答题/裁判均为 DeepSeek 家族，措辞贴原文可能让检索占便宜）：从主评测 100 题中抽 40 题（Random(7) 二次抽样，原 hr_route acc=82.5%），用**异家族模型 Kimi-K2.5**（Ali-dashscope，山大代理同端点）把问法改写成散户口语（公司名/考点/参考答案不变，如"主营业务和主要产品有哪些"→"到底是做啥生意的，主要卖些啥产品"），全部人工复核（1 题歧义手修），重跑 hr_route 同口径评测：

| 问法 | n | acc | 差值 |
|---|---|---|---|
| 原问法（年报腔） | 40 | 82.5% | — |
| 口语化改写 | 40 | **85.0%** | **+2.5pp** |

要点：

- **检索对问法皮实**：判定线是掉 ≤5pp，实际持平略升（+2.5pp，在 40 题粒度即 ±1 题内，读作"无差异"）；hybrid 的 BM25 关键词层 + 向量语义层互为兜底，口语化丢关键词时语义检索接住了；
- **逐题翻转复核**：原对现错仅 #36（答案实际正确且额外指出年报两处收入数字不一致，裁判边缘判 0）；原错现对 #12/#22（口语化后反而命中更准，说明原失误是偶发）；另有 1 题 API 空回噪声已补跑修正；
- **近亲链已切断**：改写者 Kimi 与出题/答题/裁判的 DeepSeek 非同家族，"85% 是措辞占便宜"的质疑被数据否定；
- **决策含义**：query 增强（HyDE/Multi-Query/子问题分解/Step-back）暂无必要——先测后决策，"测过所以没做"。若后续上，优先级为子问题分解（考卷双问句多）> HyDE（语域差）。

## 4. 分析结论

**结论 1：检索增益跨模型复现——消融曲线是检索的功劳，最硬的结论。**
hr → hr_route 的路由增益在三个生成器上全部成立（base +10.8pp、rank16 +11.8pp、API +7.6pp），evidence 92.5%→96.2% 与生成器无关。检索管线的价值得到跨模型验证。

**结论 2：LoRA 微调对 RAG 生成没有增益（诚实且有价值的负结果）。**
rank16 与 base 在两个配置上净差 <2pp，逐题胜负 8:7 / 4:4，误差范围内打平。原因：LoRA 按**闭卷问答**训练（往参数里背事实），而 RAG 是**开卷考试**，上下文已把答案喂到嘴边，考的是阅读理解——这个能力 base Qwen3-4B 本来就有。LoRA 的真实收益体现在两处：① 闭卷场景（此前 eval_rank 实验 rank16 远超 base）；② 答题风格（不啰嗦、直接给答案，中位长度 73 vs 206 字符）。

**结论 3：检索做好之后，4B 本地小模型 ≈ 大模型 API 的 96%。**
同子集上 83.3% vs 86.7%，差距仅 ~4pp（约 3~4 题），且集中在长表格数值读数题。含义：**RAG 系统的瓶颈在检索而非生成**——检索质量拉满后，用 4B 本地模型替换商用 API，精度损失很小，可换来私有化部署/零 API 成本。

**结论 4：剩余错误的天花板在数据侧不在模型侧。**
三方全错的 5 题 + 仅 API 对的 6 题，典型如"合并所有者权益变动表本期增减变动金额"——长表格线性化质量和二值严判（答不全计 0）是下一步优化点，换更强生成模型收益有限。

## 5. 踩坑总结（按时间顺序，均已修复）

### 坑 1：nohup 后台进程状态迷惑
**现象**：Ctrl+C 后任务"还在跑"，分不清进程状态。
**原因**：nohup + `&` 挂后台，Ctrl+C 杀不掉，SSH 断连也不退出。
**处置**：约定所有命令**单行、前台运行**，靠脚本内断点续存兜底中断风险（而不是靠进程不死）。

### 坑 2：多行命令粘贴断裂（`\` 续行符陷阱）
**现象**：命令"什么也没发生"，秒退。
**原因**：多行命令粘贴合并成一行后 `\ ` 变成转义空格，argparse 报 `unrecognized arguments`，进程秒退但 `[1] PID` 提示让人误以为已启动。
**处置**：所有交付命令一律写成单行。

### 坑 3：CUDA OOM 连环炸（首轮本地评测报废的主因）
**现象**：64~79% 的题报 `CUDA out of memory`，acc 只有 16~23%，数据作废。
**原因**：16G 卡同时装 Qwen3-4B(9G) + bge(1.3G) + reranker(2.2G) ≈ 12.5G，长表格上下文的 KV cache 需再要 1.7~2.4G 不够；且一次 OOM 后显存碎片化，后续题连环失败。
**处置**（三招）：
1. **模型生命周期管理**：先把全部检索做完 → 立即释放 bge/reranker（`del` + `empty_cache()`）→ 生成模型独享整卡（余量 3.5G→7G）；
2. 输入超 6144 token 保头保尾截断（尾部是问题不能丢）；
3. 单题 OOM 后 `empty_cache()` 清碎片，不再连环炸（残余 OOM 从 ~70 题降到 ~10 题）。

### 坑 4：Qwen3 base 默认开思考模式
**现象**：base 答案是大段 `<think>...`，512 token 上限内经常还没想完就被截断，既费显存又答不完整。
**原因**：Qwen3 chat template 默认 `enable_thinking=True`；rank16 因为学过 QA 数据风格自发不 think，base 没有。
**处置**：`apply_chat_template(..., enable_thinking=False)`。这也是首轮 base 分数（23%）远低于 rank16 假象的来源之一。

### 坑 5：裁判失败覆盖本地已生成答案
**现象**：GPU 辛苦生成的答案被 `[ERROR] ...` 抹掉。
**原因**：异常处理里 `it["answer"] = f"[ERROR] {e}"` 无条件覆盖。
**处置**：改 `setdefault`，只在没有答案时才写错误标记。

### 坑 6：gen 失败题在 judge 阶段 KeyError
**原因**：gens 里失败题只有 error 标记，没有 judge 字段。
**处置**：判分函数对 error 题显式 `judge=0` 返回。

### 坑 7：长任务中断即白算
**现象**：评测进程被杀，跑到一半的配置全部重来。
**处置**：三级断点续存——配置级跳过（非 partial 不重跑）、配置内每 20 题落盘（partial 标记）、生成答案每 10 题增量存 `gens` 区（重跑按题目文本匹配复用，`--fresh` 也不清除）。首轮 OOM 事故中 vec/hybrid/hr 结果零损失就是靠这个。

### 坑 8：评测 QA 无归属（前置坑，方案一已修）
**现象**：旧 QA 没有 source/company 字段，无法做公司路由评测，且存在"哪家公司"都说不清的题。
**处置**：重跑 gen_heldout.py 以 chunk 为锚重造 3108 条带归属 QA，旧数据作废。这是本轮所有实验的数据基础。

## 6. 工程机制沉淀（可复用）

- **两阶段评测**：`--stage gen`（纯 GPU 生成，零 API）/ `--stage judge`（纯 API 打分，零 GPU）/ `--stage all`（先集中干完 GPU 的活 → 打印 `[GPU] 全部生成完毕，显存已释放` → 再统一并行打分，打分期间不占卡）；
- **显存生命周期**：编码器（bge/reranker）在检索结束即释放，不陪跑生成阶段；
- **结果隔离**：本地评测独立目录 `eval_local/`，与 API 结果文件互不污染；裁判统一走 API 保证三组分数可比；
- 标准命令（单行、前台，`RAG-lora` 目录下）：
  - base：`CUDA_VISIBLE_DEVICES=1 python -u eval_retrieval.py --gen-backend local --no-adapter --retrieval-device cuda --configs hr,hr_route --limit 100 --workers 8 --out eval_local/results_base.json`
  - rank16：`CUDA_VISIBLE_DEVICES=3 python -u eval_retrieval.py --gen-backend local --retrieval-device cuda --configs hr,hr_route --limit 100 --workers 8 --out eval_local/results_rank16.json`

## 7. 遗留与下一步

| 优先级 | 事项 | 说明 |
|---|---|---|
| P0 | 补跑 ~10 题 OOM 题 | `max_input_tokens` 降至 4096 续跑（断点续存只重算 OOM 题），把表格里"排除 OOM"的星号去掉 |
| P1 | 裁判校准 | 用异家族 Kimi-K2.5（§3.7 已接入）当第二裁判对已判题独立重判，算交叉一致率，分歧题人工仲裁，给出裁判噪声区间（§9.1 四件事仅剩此项） |
| P1 | 表格线性化 | 三方全错题集中在长表格数值题，改进 chunk 内表格的行列结构保留 |
| P2 | 面试叙事整理 | "检索消融跨模型复现 + LoRA 开卷/闭卷分工 + 4B≈API 96%"三条主线 |
