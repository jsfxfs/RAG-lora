# held-out 评测实验设计（年报 QA：微调 vs RAG 架构选型）

> 目标：用**可控实验**证明"事实抽取类任务该用 RAG 而非微调"，把项目从
> "我调了个模型"升级成"我做了架构选型的对比实验"——简历标杆项目。

## 1. 资产现状（已具备）
| 资产 | 位置 | 用途 |
|---|---|---|
| 微调 QA 训练集 | `03_LoRA数据/qa_alpaca.json` | FT 训练（外部已完成，7078 条 / 10 家）|
| RAG 知识库（已见）| `04_RAG数据/rag_chunks.json` | RAG-on-A 的 KB（2571 块 / 10 家）|
| 年报 PDF | `01_年报PDF/`（50 份，仅 10 份已处理）| 剩余 40 份 = 候选 held-out |

## 2. 实验设计（2×2 对照）
| 查询题 | FT（权重） | RAG（检索） |
|---|---|---|
| **见过 A**（10 家）| 高（背的）| 高 |
| **未见 B**（15 家）| **低（泛化不了）** | **高** ← 命门证据 |

- **训练集 A** = 03/04 已有的 10 家 → FT 已训（本实验不重训）
- **评测集 B** = 本实验新选的 ~15 家未见公司
  - FT **零样本**答 B（它从没见过 B）→ 必然差 → 证"只会背 A"
  - RAG 的 **KB 只放 B 的 chunks** 再答 B → 必然好 → 证"事实在知识库"
- 同题 A/B 对比：`X%`(FT零样本) vs `Y%`(RAG)

### 为什么 KB=B-only 是干净设计
FT 只见过 A；RAG 只拿到 B 的文档。两边信息完全不相交，
唯一变量是**架构**。这避免了"RAG 是不是本来就更强"的杠点。

## 3. 数量与选择
- **held-out 取 ~15 家**（不是 40——40 是论文显著性，简历用不上）。
- **跨行业 / 跨难度**挑选（别只挑和训练集同类型），防"你挑了简单的"。
- `gen_heldout.py --pick 15` 默认按**板型**（主板/创业板/科创板/ST）均匀挑，保证多样；
  也可 `--manifest` 手动指定（在 README 同级放 `heldout_B/manifest.json` 写 15 个 PDF 文件名）。

## 4. 物理隔离（铁律）
- `gen_heldout.py` 的所有产物**只写 `heldout_B/`**（chunks/ + qa_alpaca.json + qa_sharegpt.json）。
- **绝不写 `03_LoRA数据/` 或 `04_RAG数据/`** —— 否则 B 混进训练集，FT 就"见过 B"了，实验作废。
- RAG 的 KB 在评测 B 时只加载 `heldout_B/chunks/`，不加载 `04_RAG数据/`。

## 5. 运行步骤（需山大代理额度，用户确认后跑）
```bash
# ① 预览将挑哪 15 家（不耗额度）
python gen_heldout.py --dry

# ② 生成 B 的 chunks(RAG KB, 本地免费) + QA(评测题, ~3750 次 API)
python gen_heldout.py --pick 15

# ③ 填好 eval_heldout.py 顶部端点后, 跑 2x2
python eval_heldout.py --all
```
- ② 约 15×250≈**3750 次 API**（vs 全 40 家的 ~10000，砍掉 2/3）。
- ③ 的 `retrieve()` 当前是关键词兜底；**正式评测前请换成本地 bge-m3 向量召回**，否则 RAG 侧不公平。

## 6. 简历子弹点（填真数字）
> - 基于 LlamaFactory 对 Qwen3-4B 做 LoRA 微调（rank 4/8/16 三组对照），在 7078 条年报 QA 上训练损失 1.344→1.262，记录显存/吞吐权衡并选定配置；
> - 设计 **held-out 评估**（10 家训练 / 15 家未见），用 LLM-as-Judge 发现事实抽取任务上微调**未见公司准确率 X% 显著低于训练公司**，本质是把知识固化进权重；
> - 据此搭建 **RAG 检索管线**（本地 bge-m3 嵌入 + 向量库，KB=年报切片），同题 A/B：RAG 未见公司准确率 Y% vs 微调 X%，实证"事实该检索、行为才该微调"；
> - 补做 500 条格式示范轻量微调（学输出结构而非事实），before/after 评测证明风格一致性提升，最终落地 RAG + 轻量风格 FT 混合架构。

> ⚠️ 第 2、3 条的 **X% / Y% 必须真跑出来填**，留空 = padding；填了 = 证据。
