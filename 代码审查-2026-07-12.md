# 代码审查简报 — 2026-07-12

审查对象：`finance_reports/` 下用户 7/12 对 `generate_qa.py` 的修改（断点续存 + 新 CLI 参数）。
`clean_report.py` 自 7/10 P0 后未改动，状态正常。

## 一、用户改了什么（`generate_qa.py`, 7/12）

1. 新增 CLI 参数：`--limit N`（仅跑前 N 份）、`--workers N`（API 并发，默认 2）、`--fresh`（忽略已有产出重跑）。
2. 新增**断点续存**：每跑完一份即落盘 `qa_alpaca/qa_sharegpt/rag_chunks`；重跑时按 `source` 跳过已完成 PDF。
3. docstring 同步更新，说明全量带续存、已完成的自动跳过。

## 二、验证结果

| 检查项 | 方法 | 结果 |
|---|---|---|
| 语法 / 导入 | `py_compile` + `import` | ✅ OK |
| `.env` 字段 | 比对 `ANTHROPIC_BASE_URL/AUTH_TOKEN/DEFAULT_HAIKU_MODEL` | ✅ 三 key 齐全 |
| 断点续存正确性 | 沙箱复制 2 份 PDF + 假 `call_model`，真实跑 3 遍（`--limit 2 --fresh` → `--limit 2` → 无参） | ✅ 第1次 454 QA/470 块；第2、3次均正确跳过，**零重复、零遗漏** |
| 并发（`--workers`） | 默认 2 跑通，输出交错但计数正确 | ✅ |

**结论：用户新增的逻辑无 bug，断点续存行为正确。**

## 三、需要决策的问题（非代码 bug）

### ⚠️ 03 / 04 目录现在都是同一份数据的完整副本
当前 `03_LoRA数据/` 和 `04_RAG数据/` 各含 `qa_alpaca.json` / `qa_sharegpt.json` / `rag_chunks.json`（均为 pilot 1 份 = 626/626/223，且字节级相同）。

但脚本的架构约定是：
- `03` 只写 `qa_alpaca` + `qa_sharegpt`（LoRA 数据）
- `04` 只写 `rag_chunks`（RAG 数据）

即：03 里多出的 `rag_chunks.json`、04 里多出的 `qa_alpaca/qa_sharegpt` 都是**脚本不会维护的孤儿副本**。一旦真正全量跑，只有 canonical 那侧会被更新，另一侧立刻过期，接手者会困惑"哪份是真的"。

**两种修法，二选一：**
- **A（推荐，保持文档架构）**：删掉孤儿文件，恢复 `03=qa 两件套` / `04=rag 单件` 的干净拆分。
- **B（若你想要双目录都自包含）**：改脚本让它在两个目录都写全量三件套，并在 `项目状态交接文档.md` 里注明这是有意设计。

> 注意：删 / 改前请确认这些确实只是 pilot 副本、没有别处依赖。

## 四、次要提醒（设计层面的 footgun，非当前问题）

1. **"已完成"信号只存在于 `rag_chunks.json`**：续存靠读 rag 的 `source`。如果有人手改或误删 `rag_chunks.json`，续存会误判（已完成的被重跑 / 或找不到）。属设计取舍，知道即可。
2. **`--fresh` 不清理 `02_文本抽取/` 的旧 `_clean.txt/_tables.json/_chunks.json`**：当目标子集缩小（如之前跑过全 50、现在 `--fresh --limit 5`）时，02 里其余 45 份的旧产物会残留。一般无害，重跑会覆盖对应份。
3. 单份报告若抽取为**空**（0 块），其 `source` 不会进 rag → 续存不会标记为"已完成"，每次重跑都重试。真实年报不会触发，仅理论边界。

## 五、下一步建议

- 先定 §三 的 A/B，清理目录冗余。
- 若走 A：确认后删除 `03/rag_chunks.json`、`04/qa_alpaca.json`、`04/qa_sharegpt.json`。
- 全量 50 份仍待你确认山大代理额度后开跑（`generate_qa.py` 不带 `--pilot`，带断点续存，约 1.1 万次调用）。
