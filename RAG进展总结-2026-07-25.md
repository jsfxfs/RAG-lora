# RAG 系统进展总结 — 2026-07-25 / 07-26

> 接续《项目状态交接文档.md》（数据管线部分）与《heldout_README.md》（评测集部分）。
> 本文档记录 2026-07-25 这一轮「RAG 问答链路搭建 + 检索增强」的工作内容、现状、已知问题与后续方向。

---

## 1. 本轮做了什么

### 1.1 代码整理（把散在评测脚本里的能力沉淀成可复用模块）

| 文件 | 性质 | 内容 |
|---|---|---|
| `rag/llm.py` | 新增 | 生成后端统一封装：`.env` 读取 / `post_messages`（anthropic 兼容 API）/ `APIClient` / `LocalLLM`（Qwen3-4B + 可选 LoRA） |
| `rag_chat.py` | 新增 | RAG 问答入口：检索 → 拼 prompt → 生成。支持 `--backend api/local`、`-q` 单问 / 交互模式、`--show-hits`、`--index indexA` 切 A 侧 KB |
| `eval_heldout.py` | 重构 | 改为复用 `rag.llm`（净减 ~90 行），删除死代码（`retrieve()`/`load_chunks()`），评测行为不变 |
| `rag/retrieve.py` | 修复 | 重复 `model.eval()`、meta 重复读取 |

### 1.2 检索增强（本轮核心）

| 文件 | 内容 |
|---|---|
| `rag/retrieve.py` → `HybridRetriever` | BM25（jieba 分词，`rank_bm25`，内存级秒建）+ 向量双路各取 20 候选，RRF（倒数排名融合，rrf_k=60）合并；hits 带 `vec_rank`/`bm25_rank` 双路排名，可解释 |
| `rag/rerank.py` → `Reranker` | bge-reranker-large 交叉编码器精排（~0.3B 判别式小模型，CPU 跑），对粗排 top-20 逐对打分取 top-k；模型已缓存至 `models/BAAI/bge-reranker-large` |
| `rag_chat.py` | 接入 `--hybrid` / `--rerank` / `--candidates` 开关，可自由组合 |

### 1.3 环境/配置

- 山大代理旧 token 失效（LiteLLM 报 `token_not_found_in_db`），已换新 key 并同步到 `RAG-lora/.env` 的 `ANTHROPIC_AUTH_TOKEN`，实测 HTTP 200。
- llf 环境新装 `rank_bm25`。
- 运行约束：**生成一律走 API（DeepSeek-V4-Flash）**，本地 Qwen3-4B 推理代码保留可复用但暂不运行（GPU 8 卡均被占用，4B bf16 需 ~9G 显存）。

---

## 2. 当前现状（已验证）

### 2.1 完整链路

```
问题 → [召回] bge向量 / +BM25混合(RRF) → [可选精排] bge-reranker top-20→top-6
     → [生成] 山大代理 DeepSeek-V4-Flash（或本地 Qwen3-4B+LoRA，代码就绪未跑）
     → 答案（带来源引用 [N]）
```

常用命令（llf 环境：`/data1/jiajun/.conda/envs/llf/bin/python`）：

```bash
cd RAG-lora
python rag_chat.py -q "..."                          # 纯向量 + API
python rag_chat.py --hybrid --rerank --show-hits -q "..."   # 全增强
CUDA_VISIBLE_DEVICES=N python rag_chat.py --backend local -q "..."  # 本地LoRA(有空卡时)
```

### 2.2 验证证据（面试可讲）

1. **混合召回的价值**：查"股票代码300904"，纯向量 top-4 全部跑偏（召到别家公司利润表——数字型 query 向量语义区分度差）；BM25 路第 1 名精确命中威力传动"股票代码 300904"块。
2. **精排的价值**：20 个混合候选经 reranker 后，威力传动目标块 rr=0.96 升至第 1，跑偏块压到 0.19 以下。
3. **端到端正确性**："威力传动2025年归属于上市公司股东的净利润" → 答出 **-93,767,876.98 元** 并标注来源片段，与 `heldout_B/chunks/11_300904_威力传动_*_chunks.json` 原文核对一致（非幻觉）。
4. **防幻觉行为**：检索没召到正确块时，模型明确拒答"无法基于现有片段回答"，而不是编数字。

### 2.3 各组件状态一览

| 组件 | 状态 |
|---|---|
| B 侧索引（heldout_B 15 家，~4530 块） | ✅ `rag/index.faiss` 已建，检索正常 |
| A 侧索引（04_RAG数据，10 家 2571 块） | ✅ 07-26 已建（`rag/indexA.faiss`，CPU 编码，公司元数据齐全） |
| 混合召回 / 精排 | ✅ 已验证；reranker 模型已本地缓存 |
| API 生成 | ✅ 新 token 有效 |
| 本地 LoRA 生成（Qwen3-4B + sft_rank16） | ✅ 代码就绪（CPU 冒烟通过）；GPU 推理待有空卡 |
| eval_heldout 2x2 评测 | ✅ 重构后可跑（token 恢复，judge 阶段解除阻塞） |

---

## 3. 已知问题（如实记录）

1. ~~**多约束绕弯问法会翻车**~~ ✅ **07-26 已用公司路由解决**（见 §6.1）：原翻车 query“股票代码300904的公司的归母净利润”现已正确答出 -93,767,876.98 元。
2. **剩余 25 家年报未处理**（2026-07-25 核实，交接文档 §3.6“A 侧仅 1 家 pilot”已过时）：实际 A 侧已是 **10 家**（7078 QA / 2571 块，sft_rank16 即用此训练），加 B 侧 15 家共处理 25/50 份。现有 10+15 已构成完整实验闭环，剩余 25 家属可选扩展（跑完需重训 LoRA + 重建索引 + 重跑评测，边际收益低）。
3. ~~**heldout_B 的 QA 无 `source` 字段**~~ ✅ **07-27 已用方案一彻底解决**（见 §7）：新 QA 100% 带 source/company，问题 99.9% 含公司名。
4. ~~**检索增强未量化评测**~~ ✅ **07-27 已完成 4 配置消融**（见 §7.3）：vec 50% → hr_route 85%。
5. **GPU 长期被占**：本地 LoRA 生成后端一直没做过 GPU 实测（代码路径与 eval_heldout 原逻辑一致，风险低，但未验证）。

---

## 4. 后续完善方向（按优先级建议）

| 优先级 | 事项 | 说明 |
|---|---|---|
| P0 | **公司路由/元数据过滤** | 从 query 正则识别股票代码/公司简称（chunk 的 `company` 字段现成），命中则只在该公司块内检索；直接解决问题 3.1，~40 行 |
| P0 | **检索增强量化评测** | 用 heldout_B 3187 条 QA（或抽样）对比 纯向量 / +混合 / +精排 的答案正确率（judge 现成）与检索命中率，把"增强有效"从单例变成数字 |
| P1 | **重跑 2x2 对比表** | eval_heldout 的 RAG 侧接入 hybrid+rerank（目前仍是纯向量），FT零样本 vs RAG 的差距会更显著 |
| P3 | **（可选）处理剩余 25 份年报** | 非必需：现有 A(10家)+B(15家) 实验闭环已完整；若做“训练规模 vs 背诵效果”曲线实验才值得跑，需配套重训+重评 |
| P2 | **Gradio 演示页** | 提问 → 召回片段（公司/双路排名/精排分） → 答案，面试演示直观，~100 行 |
| P2 | **本地 LoRA 后端 GPU 实测** | 等空卡，`CUDA_VISIBLE_DEVICES=N python rag_chat.py --backend local` 跑通一次即可 |
| P3 | heldout_B QA 补 `source` 字段 | 需改 `gen_heldout.py` 重跑 15 家（耗 API），对当前评测不阻塞 |

---

## 5. 一句话现状

> 数据（B 侧 KB + 3187 评测 QA）、检索（向量/混合/精排三档）、生成（API 通、本地代码就绪）、评测框架（2x2）全部打通；**下一步最有价值的两件事：公司路由解决跨公司串扰、用现成评测集把检索增强的收益量化成数字**。

---

## 6. 2026-07-26 增量进展（公司路由 + indexA + 量化评测首轮）

### 6.1 完成：公司路由（P0-1）✅

- `rag/retrieve.py`：`FAISSRetriever.__init__` 建两张路由表（company→块id 列表、股票代码→company，代码从记录 `file` 字段解析）；新增 `detect_company(query)`（优先 6 位代码正则、其次简称子串，去空格兼容“五 粮 液”）；`search(company=...)` 限定该公司块内检索（向量路用 faiss reconstruct 取子集精确算，BM25 路限定候选池），识别不到传 None 行为不变。
- `rag_chat.py`：默认自动路由（打印 `[路由] 公司名`），`--no-route` 可关。
- **验证**：原翻车 query“股票代码300904的公司2025年归母净利润”→ 路由锁定威力传动 247 块，6 候选全部命中本公司，正确答出 **-93,767,876.98 元**（此前拒答）。识别测试：代码/简称/无公司题均符合预期；已知局限：跨公司比较题只识别到单家（二期再做多公司并行检索）。

### 6.2 完成：indexA 建成（CPU，零 API）✅

- 修 `rag/build_index.py` `load_chunks`：合并式 chunks（每条带 `source`）优先从 source 解析公司，避免 04_RAG数据/rag_chunks.json 整库被归到“rag”一家；B 侧目录式回归验证不受影响。
- `rag/indexA.faiss` + `indexA.json`：2571 块 / 10 家 / 代码表齐全，CPU 编码 ~11 分钟（实测 64 块 15.9s）。`rag_chat.py --index indexA` 即用，路由检索验证通过。

### 6.3 进行中：检索量化评测（P0-2）⚠️ 首轮受阻，待决策

- 新建 `eval_retrieval.py`：同一抽样 QA（seed=42，默认 100 题）对比 4 配置 vec / hybrid / hr(混合+精排) / hr_route(+路由)；指标：acc（LLM-as-Judge 对照参考答案）+ evidence（参考答案数字是否出现在检索上下文，免费代理指标）；生成/裁判全 API，不碰本地 LLM 推理。
- **首轮事故**：API 偶发读超时把线程池炸掉，且旧代码最后才统一写盘 → 已完成的 vec 结果只留在日志里。已修：重试 3 次+超时 120s、单题失败计 0 不中断、每配置跑完即落盘 `eval_retrieval_results.json`。
- **已有数字**（日志 `eval_retrieval_run.log`）：vec 基线 100 题 **acc=49.0%，evidence=59.3%**；4 题冒烟 vec 25% vs hr_route 75%（样本太小仅作信号）。完整 4 配置对比尚未跑完（第二轮中途应用户要求停止）。
- **关键发现：vec 基线低的主因不在检索算法，而在评测题本身**——heldout QA 大量问题不带公司名（如“公司的盈利模式是什么？”），15 家共库检索无从定位目标公司，检错公司 judge 必判 0，把所有配置的绝对分都压低（QA 生成时去公司指代+无 source 字段的历史遗留，即 §3.3）。相对比较仍有效，绝对值失真。

### 6.4 待决策：评测 QA 归属问题的两条修复路线（→ 07-27 已选方案一并完成，见 §7）

| 方案 | 做法 | 成本 | 风险 |
|---|---|---|---|
| 一：重跑 QA 生成 | 改 `gen_heldout.py`（存 source + 问题带公司名）重跑 15 家 | ~4500 次 API，数小时；**旧评测结果全部作废重跑** | 新旧不可比 |
| 二（推荐先试）：BM25 反向补归属 | 参考答案是块原文摘抄（“根据【块10】原文…”），用 BM25 拿答案反查 4461 块→推断每题归属公司；低置信题丢弃 | 零 API，纯 CPU 几分钟，~50 行 | 需抽查归属准确率，不准则弃用 |

方案二成功后可解锁：评测时问题拼公司名（绝对分真实化）、检索 recall@k 直接可算、per-company 打分。

### 6.5 下次继续时的入口

1. 先定 §6.4 方案（建议先试方案二，看覆盖率和抽查再定）；
2. 补归属后重跑 `eval_retrieval.py`（已具备断点友好：重试+逐配置落盘），出 4 配置对比表；
3. 后续仍排队：2x2 表 RAG 侧接入 hybrid+rerank+route 重跑、Gradio 演示页、本地 LoRA 后端 GPU 实测（等空卡）。

运行约束不变：生成/裁判只走 API（DeepSeek-V4-Flash），本地只跑 bge/reranker 等 CPU 小模型，不跑 Qwen3-4B 推理。

---

## 7. 2026-07-27 增量进展（方案一落地 + 4 配置消融全部跑通）

### 7.1 完成：方案一——重造评测 QA（归属缺失彻底修复）✅

- **改造 `gen_heldout.py`**：① prompt 强制每个问题写出公司简称（禁止“公司/该公司”指代）；② 每条 QA 落盘带 `source`（来源 PDF）+ `company` 字段；③ 断点续存改为 `heldout_B/qa_done.json` 进度表（与 chunks 解耦，chunks 已有则直接复用不重抽 PDF）；④ 每 10 组分段落盘，单组失败重试 3 次，8 线程并行。
- **重跑 15 家产出**：新 QA **3108 条**（旧 3187 条量级一致）；`source`/`company` 覆盖 **100%**；问题含公司名 **99.9%**（3106/3108，仅 2 条行业级事实题不针对单公司，可忽略）；15 家全覆盖（133~320 条/家）。
- **旧数据处置**：旧 QA 备份在 `heldout_B/qa_alpaca.json.bak_nosource`（及同名 sharegpt）；**基于旧考卷的一切评测数字作废**（含 §6.3 的 vec 49%），新旧不可比。B 侧 chunks/索引未动。

### 7.2 完成：`eval_retrieval.py` 健壮性升级 ✅

- 配置级断点续存：结果文件里已完成的配置自动跳过（`--fresh` 强制重跑；**续跑时切勿带 `--fresh`**）；
- 配置内每 20 题分段落盘（`partial` 标记，续跑时该配置整体重跑）；已有：单题重试 3 次+超时 120s、失败计 0 不中断、errors 计数可审计。
- 实战验证：中途进程被杀一次（hr_route 评测刚开始），vec/hybrid/hr 三配置零损失，续跑只补 hr_route，断点机制按预期工作。

### 7.3 核心成果：4 配置消融结果（新考卷 100 题，seed=42，errors 全为 0）

| 配置 | acc | evidence | 较上一档 | 修复/退化（逐题对比） |
|---|---|---|---|---|
| vec（纯向量） | 50.0% | 84.9% | 基线 | — |
| hybrid（+BM25/RRF） | 67.0% | 84.9% | **+17** | 修20 / 退3 |
| hr（+reranker） | 77.0% | 92.5% | **+10** | — |
| hr_route（+公司路由） | **85.0%** | **96.2%** | **+8** | 修15 / 退7（routed=100/100） |

- **每一级增强都实打实涨分**，50→85 共 +35 个点；证据召回率 84.9%→96.2%。
- 错题归因（vec 基线 50 错题手工分析）：证据已召回仍错 24（表格读取失败/答不全被严判/裁判误判）、无数字题错 18、检索未召回仅 8——**数据修补后瓶颈已从检索转移到生成读表+二值严判**。
- 裁判方式（面试可能被问）：LLM-as-Judge 二值 1/0，数值错/拒答/答不全均 0 分，表述不同不扣分；另有 evidence（参考答案 ≥4 位数字是否出现在检索上下文，免 API）单独衡量检索。已知裁判缺陷：① 生成对但裁判调用失败会冗判 0（本轮 errors=0 未发生）；② 参考答案只覆盖出题块，模型引其它块的正确信息可能被判错（如五粮液市值管理制度题确认被冗判）。
- 备份：`backups/20260727_eval4配置_新QA/`（结果 json+日志+新 QA 全套）。

### 7.4 运维备忘（本轮踩坑沉淀）

- 长任务标准姿势：`nohup python -u xxx.py > xxx.log 2>&1 &`；看存活 `pgrep -af xxx.py`，看进度 `tail -f xxx.log`，真杀用 `kill PID`（Ctrl+C/关终端/断 SSH 均无效）。
- **启动前先 `pgrep` 确认无同名进程**：本轮曾双进程同写一个日志/结果文件（日志乱码），杀新留旧后无损恢复。
- 评测/生成日志均在 `eval_retrieval_run2.log` / `gen_heldout_rerun.log`。

### 7.5 下一步（按优先级）

| 优先级 | 事项 | 说明 |
|---|---|---|
| P0 | **裁判校准** | 抽 judge=0 样本人工复核误判率；judge prompt 加“包含参考核心事实即判 1”；只重跑裁判比重跑全场便宜一半 |
| P1 | **表格块线性化优化** | 证据已召回仍错的大头是表格读取；每行带行名+表头可治 |
| P1 | 2x2 对比表重跑 | eval_heldout RAG 侧接入 hr_route（现仍纯向量）+ 新考卷 |
| P2 | Gradio 演示页 / ~~本地 LoRA GPU 实测~~ ✅ 07-27 已完成（见 §8） | 演示页未做 |
| P3 | recall@k 直接量化 | 新 QA 有 source，可精确算检索命中率（免 API，纯本地） |

### 7.6 一句话现状（接替 §5）

> 评测数据彻底修复（3108 条带归属新 QA），检索增强收益已量化成完整消融曲线 **50% → 67% → 77% → 85%**（向量→+BM25→+精排→+路由）；下一刀应切在裁判校准与表格线性化，而非继续堆检索。

---

## 8. 2026-07-27 增量进展 II（本地模型 GPU 实测：base vs rank16 vs API 三方对比）

> 详细数据、踩坑与结论见独立汇报：**`实验汇报-本地模型vsAPI做RAG-2026-07-27.md`**。本节只留摘要。

- **实验**：卡 1 跑 Qwen3-4B 纯 base、卡 3 跑 base+LoRA(sft_rank16)，各评 hr / hr_route 100 题（同考卷 seed=42，裁判统一 DeepSeek API）；结果在 `eval_local/results_base.json` / `results_rank16.json`。
- **净 acc（排 ~10 题 OOM）**：hr_route 上 base **83.3%** / rank16 **82.6%** / API 同子集 **86.7%**。
- **三条结论**：① hr→hr_route 路由增益跨三个生成器全部复现（+8~12pp），消融曲线确系检索功劳；② **LoRA 对 RAG 生成无增益**（逐题胜负 4:4 打平）——LoRA 价值在闭卷背诵与答案风格（中位 73 vs 206 字符），开卷阅读理解 base 本来就会；③ 检索拉满后 4B 本地模型 ≈ API 的 96%，瓶颈在检索不在生成。
- **关键踩坑修复**（详见汇报 §5）：16G 卡 OOM 连环炸 → 检索完即释放 bge/reranker + 输入 6144 截断 + OOM 后清缓存；Qwen3 base 默认思考模式 → `enable_thinking=False`；`eval_retrieval.py` 新增 `--stage gen/judge/all` 两阶段（GPU 活干完即释放显存，API 打分不占卡）、`--gen-backend local`、`--retrieval-device cuda`。
- 遗留：~10 题最长上下文 OOM 未答（P0 补跑：`max_input_tokens` 降 4096 断点续跑即可）。

---

## 9. 2026-07-27 项目全面盘点：缺口清单（接替 §7.5 作为最新行动指南）

> 定位：实验闭环完成度已高，缺的是**评测效度加固**和**对外呈现层**。

### 9.1 第一档：评测效度硬伤（面试被内行追问会露馅，优先做）

| # | 缺口 | 问题 | 补救方案 | 成本 |
|---|---|---|---|---|
| 1 | ~~**考卷近亲繁殖**~~ ✅ **07-28 已完成**（见 §9.8） | QA 由 DeepSeek 从 chunk 生成（措辞贴原文，检索占便宜），裁判又是同一模型——出题人/考生/阅卷人一家子，85% 可能高估真实口语提问表现 | 抽 30~50 题人工改写成口语问法重跑 hr_route；掉 ≤5pp 即可证皮实 | 1 天 |
| 2 | ~~**无拒答/幻觉评测**~~ ✅ **07-28 已完成**（见 §9.7） | 考卷全是有答案的题，从未测过库外问题会不会胡编；金融场景致命项 | 造 30 题库外问题（别家公司/编造事项）量化拒答率与幻觉率；prompt 加“无依据请明说” | 1 天 |
| 3 | **裁判未校准** | 二值严判已知有冤案（五粮液题），裁判噪声区间不明，85% vs 83.3% 显不显著说不清 | 抽 30 题人工对照裁判打分给一致率；标注 100 题抽样置信区间（±7pp 量级） | 半天 |
| 4 | ~~**recall@k 未直接算**~~ ✅ **07-28 已完成**（见 §9.6） | evidence 只是代理指标；新 QA 有 source 字段，检索命中率可免 API 纯本地精确算 | 四档配置各算 recall@6/@20，给消融曲线最干净的注脚 | 半天，零成本 |

### 9.2 第二档：已知短板，做了有明确收益

- **表格线性化**：三方全错题的大头，每行带表头+行名可治；治好 85% 还能涨；
- **~10 题 OOM 补跑**：`max_input_tokens` 降 4096 断点续跑，去掉结果星号；
- **2x2 对比表重跑**：eval_heldout 的 RAG 侧仍是纯向量+旧考卷，“FT vs RAG vs FT+RAG”这张核心表还是旧数字；
- **chunk 策略无消融**：块大小/重叠只有一版拍脑袋参数，被问“为什么这么切”无数据支撑。

### 9.3 第三档：呈现与工程化（时间富余再做）

- **Gradio 演示页**（本档唯一建议做的）：提问→召回片段（公司/双路排名/精排分）→带引用答案，~100 行；
- 引用准确性验证（答案里 [N] 标注对不对从未验过）；FastAPI 服务化；索引增量更新；剩余 25 家年报扩展（边际收益低）。

### 9.4 建议执行顺序（已被 §10.8 接替，保留存档）

~~recall@k（免费半天）~~✅ → ~~拒答评测（1 天）~~✅ → ~~口语化改写测试（1 天）~~✅ → 裁判校准（半天）→ 表格线性化 → Gradio。做完前四项，项目就没有“被一句话问穿”的软肋。

### 9.5 一句话现状（接替 §7.6）

> 数据、检索消融、三方模型对比、耗时基准全部闭环（详见《实验汇报-本地模型vsAPI做RAG-2026-07-27.md》）；剩余风险集中在**评测效度**（~~考卷近亲繁殖~~✅、~~无拒答评测~~✅、裁判未校准、~~recall 未直算~~✅），只剩裁判校准半天，之后只剩呈现层（Gradio）。

### 9.6 2026-07-28 完成：recall@k 直算 ✅（§9.1 #4）

- **新目录规范**：§9.1 四件事的产物统一进 `eval_validity/`（`01_recall/`、`02_refusal/`、`03_colloquial/`、`04_judge_calib/`），不再往根目录撒文件；
- **脚本**：`eval_validity/01_recall/eval_recall.py`，纯本地零 API，CPU 十几分钟，与 eval_retrieval 同 seed 同子集；结果 `recall_results.json`（含逐题明细）；
- **对齐已核实**：QA 答案【块N】是 gen_heldout 的 1-based 编号，索引 chunk_idx 是同一 chunks 文件的 0-based 位置，gold = N-1 无错位；
- **结果**（详细表与四个要点见实验汇报 §3.5）：doc@20 全档 100%（找公司早饱和）；**chunk@6 = 42.9→52.4→66.7→81.0%，与 acc 50→67→77→85 几乎同步**，坐实“acc 增益来自检索命中率”；精排管排序（chunk@20 不变、@6 +14pp）、路由管候选集（chunk@20 +19pp）；另发现 reranker 会跨公司串扰（hr doc@6 92% < hybrid 94%），路由恰好治此；
- **口径提醒**：chunk 级分母仅 21 题（答案带块号的那部分），区间宽，引用时说趋势不说绝对值；doc 级分母 100 可直接引用。

### 9.7 2026-07-28 完成：拒答/幻觉评测 ✅（§9.1 #2）

- **产物**：`eval_validity/02_refusal/`（考卷 questions.json 30 题 + 脚本 eval_refusal.py + 结果 refusal_results.json + run.log）；
- **考卷设计**：15 库外公司 + 15 编造事项（每家 1 题，专挑存在必披露的大事）；造题后对 B 侧语料做了关键词自检，3 题命中处核实均为无害上下文，顺势标为硬负例（最毒 #28：语料释义表真有“Enchem 指 韩国亿恩科”）；
- **结果**（hr_route + API 生成，详见实验汇报 §3.6）：**拒答率 100%（30/30），幻觉率 0%**，人工全量复核与裁判一致；硬负例全过（#23 未把真实巴西铜金矿当智利锂矿，#21 还引原文反驳）；路由零误触发；现有 prompt 未加防幻觉提示即达此水平，--guard 对照组无需跑；
- **口径提醒**：生成器为 API；本地 Qwen3-4B 拒答能力未测（无卡），如面试被问可如实说明并列为可选补测。

### 9.8 2026-07-28 完成：口语化改写测试 ✅（§9.1 #1）

- **产物**：`eval_validity/03_colloquial/`（original_40.json 原题 + rewrite_40.py 改写脚本 + rewritten_40.json 对照 + eval_colloquial.py + colloquial_results.json + 两份 log）；
- **方法**：从主评测 100 题（Random(42)）二次抽样 Random(7) 取 40 题，用**异家族 Kimi-K2.5**（`Ali-dashscope/Kimi-K2.5`，山大代理同端点仅换模型名）改写成散户口语，公司名/考点/参考答案不变，脚本自检公司名保留 40/40，人工逐条复核（#36 "挣了多少钱"歧义手修为保留"营业收入"字样）；检索全程 CPU；
- **结果**（详见实验汇报 §3.7）：原问法 82.5% vs 口语化 **85.0%（+2.5pp，持平略升，判定线掉 ≤5pp），检索对问法皮实**；翻转题全部复核：无一例是口语化导致检索失败（#36 裁判边缘判、#12/#22 原失误偶发、1 题 API 空回已补跑）；
- **决策含义**：query 增强（HyDE/Multi-Query/子问题分解/Step-back）均未做且暂无必要——本测试就是它们的动机实验，结论是"测过所以没做"；若后续要上，优先子问题分解（考卷双问句多）> HyDE（语域差）；
- **意外收获**：项目接入了第二家异家族模型，任务④裁判校准可直接用 Kimi-K2.5 当第二裁判做交叉一致率。

## 10. 后续路线图：生产化视角（2026-07-28 讨论定案）

> 定位：§9.1 评测效度四件事收尾后，项目从“把分数做确”转向“把系统做真”。本节是讨论定案的设计与决策记录，**写决策不等于写代码**：部分条目价值在论证而非实现，面试可直接引用。

### 10.1 错题归因分析（一切增强的前置，半天、零成本）

关键数字缺口：hr_route 的 **evidence 覆盖率 96.2% vs acc 85%，中间 ~11pp 是“检索已命中但答案仍错”的区域**，里面混着四种性质完全不同的错误：

| 归因类别 | 含义 | 对应解法 |
|---|---|---|
| 内在幻觉 | 上下文里有对的数，模型忽略或编了别的 | 数字回查校验器（§10.2） |
| 表格读数错 | 线性化质量差，看得见但读串行 | 表格线性化（§9.2 已列） |
| 双问半对 | 复合问题只答对一半被二值严判判 0 | 子问题分解（§10.3） |
| 裁判冤案 | 答案对但裁判判 0（#36 活例） | 裁判校准（§9.1 #3） |

做法：从 `eval_retrieval_results.json` 筛 hr_route 全部错题（含 evidence=1 & judge=0），人工逐题标四类，产出占比表；**占比表直接决定 §10.2/10.3 做哪个**——延续“先测量→数据决策→验证”方法论。产物进 `eval_validity/05_error_analysis/`。

### 10.2 幻觉处理（内在/外在分治）

- **外在幻觉**（上下文根本没相关信息）：✅ 已闭环——任务②库外 30 题拒答率 100%、幻觉率 0%（§9.7）；可选补测：有卡后用同一张考卷测本地 Qwen3-4B 的拒答能力；
- **内在幻觉**（有正确上下文但模型忽略）：藏在 §10.1 的 ~11pp 里，占比待归因。若坐实，上**数字回查校验器**：生成后用纯字符串匹配回查答案中每个金额/百分比是否在检索上下文中出现，查不到标“⚠️ 该数字无出处”。零 API 成本、几十行代码、拦截率可量化；金融场景“数字必须有出处”的设计动机自解释。比 CoVe（再调一轮 LLM 自校验）便宜一个数量级，先上破车再说。配套：生成 prompt 的“可引用来源”改“必须引用 [N]”，引用准确性也从未验过（§9.3 遗留），可一并验。

### 10.3 Query 拆解（子问题分解，条件触发）

口语化测试（§9.8）否定的是“问法鲁棒性”动机，**没否定“复合问题证据分散”动机**：考卷双问句多（“X 是多少？原因是什么？”），两问证据可能不在同一 chunk，top-6 挤不下。触发条件：§10.1 归因里“双问半对”占错题大头。实现从俭：按“？”分句规则拆（不动用 LLM），子 query 各检 top-3 合并去重再生成；评测同 100 题 hr_route ± decompose 对比。HyDE/Multi-Query/Step-back 维持“测过暂不做”（§9.8）。

### 10.4 索引增量更新（增/删/改）

场景特性先行：**年报是不可变快照**，更新形态天然 append-only（新年份/新公司）；真正的“改”只有勘误/重述，且以整份文档替换出现 → **更新单元是文档不是 chunk**。

| 操作 | 当前规模（4461 块） | 生产方案 |
|---|---|---|
| 新增 | 全量重建（CPU 十几分钟，不过度工程） | 新文档切块向量化后 `faiss.add()` 追加 + records 追加；BM25 内存结构直接重建 |
| 删除 | 同上 | FAISS 平铺索引不支持真删 → **软删除**：records 标 deleted、检索后过滤，定期 compact 全量重建 |
| 修改 | 同上 | = 软删旧文档全部 chunk + 追加新文档（永不改单 chunk） |

基建已有一半：`manifest.json` 补 `file → content_hash → chunk_id 区间` 映射即成增量账本（哈希变→文档级替换；没变→跳过）。若动手：`update_index.py` 一个入口管增/替换，纯本地零占卡，半天。

### 10.5 时效与错误内容治理

- **过时**：解法不是删而是**时间感知检索**：chunk 元数据补 `report_year`（文件名自带日期，白捿）；query 带年份→硬过滤，不带→默认最新年份且答案强制标注“数据来自 XX 年报”；多年份同时命中时 prompt 要求按年份分开陈述；
- **错误定位**：两条链——① 抽取错误（PDF 解析串行）靠上游质检（qc_data.py）+ 数字校验器自动探测可疑 chunk；② 生产反馈回路：用户点踩 → 落库 query + 命中 chunk_id → 沿 `file + chunk_idx` 溯源到 PDF 原页。records 结构已具备溯源链，缺的只是引用强制化（§10.2 配套）。

### 10.6 GraphRAG：评估后决定不做（决策记录）

三层论据，层层有自己的数据：
1. **查询模式不匹配**：GraphRAG 价值在跨文档多跳与全局摘要；本项目 95%+ 查询是单公司单指标，公司路由已将检索切到单文档内，零跳；recall 坐实 doc@20=100%，“找文档”根本不是瓶颈，而图解决的恰是找文档/找关系；
2. **成本与更新冲突**：建图要 LLM 抽全量实体关系（token 成本数十倍）；增量更新是 GraphRAG 最大痛点（新文档要合并子图、重算社区），与 §10.4 的 append-only 更新需求直接打架；
3. **唯一潜在图场景有更便宜替代**：年报里真像图的只有关联交易/股权穿透网络，强 schema 数据用结构化抽取成表 + 过滤查询比图数据库实惠得多。

面试口径：“按查询模式和 recall 数据评估过，GraphRAG 不适配此场景”——与 query 增强的“测过所以没做”同一套方法论。

### 10.7 其他生产化补丁（只写决策，暂不实现）

- **评测回归集**：把 100 题主评测 + 30 题拒答考卷固化为回归集，任何检索/prompt/索引改动后必跑，acc 掉 >3pp 或幻觉率 >0 即回滚——CI 思维，现有脚本零改动即可充当；
- **服务化与监控**（§9.3 已列 FastAPI）：生产还需在线指标——检索分数分布漂移监控（top-1 分数均值骤降 = 索引/查询分布变了）、拒答率监控（骤升 = 库覆盖不足）；
- **安全**：金融场景防 prompt 注入（用户 query 拼进 prompt 前做清洗）；年报是公开数据无合规风险，但反馈回路落库的用户 query 需脱敏；
- **成本账**：已有耗时基准（§8），可补 token 成本估算：本地 4B（零 API 成本，acc 83.3%） vs API（acc 86.7%），“精度损失 ~3pp 换私有化+零成本”已是现成结论。
- **对标标准框架（RAGAS 交叉验证，可选）**：手搓的 recall@k / evidence / 拒答 / 裁判校准分别对应业界标准的 Context Recall / Faithfulness / 幻觉 / kappa（详见学习文档 05 章 §7）；可把 **RAGAS 接进来跑一轮**（半天、纯 API），与手搓指标交叉验证——既证明轻量版可靠，又展示会用标准工具。

### 10.8 优先级总装（接替 §9.4 作为最新行动清单）

| 顺序 | 事项 | 成本 | 产出形态 |
|---|---|---|---|
| 1 | 裁判校准（§9.1 收官，Kimi 第二裁判） | 半天，纯 API | 交叉一致率 + 噪声区间，`eval_validity/04_judge_calib/` |
| 2 | 错题归因分析（§10.1） | 半天，零成本 | 四类占比表，决定 3/4 做哪个，`eval_validity/05_error_analysis/` |
| 3 | 数字回查校验器 或 子问题分解（归因定） | 各 ~1 天 | 同 100 题对比重跑闭环 |
| 4 | 表格线性化（§9.2 遗留，归因大概率会再次指向它） | 1~2 天 | 重建索引 + 回归集验证 |
| 5 | 增量更新 update_index.py + report_year 元数据（§10.4/10.5） | 各半天 | 生产化基建，顺手进回归集 |
| 6 | Gradio 演示页（§9.3） | 1 天 | 呈现层收尾 |

GraphRAG（§10.6）、HyDE/Multi-Query/Step-back（§9.8）：决策记录在案，不排期。
RAGAS 交叉验证（§10.7）：可选加分项，半天纯 API，不占正式排期，得空或面试需要时再做。
